# Copy to .env and fill in. Only ANTHROPIC_API_KEY is required.
ANTHROPIC_API_KEY=
# Optional: best-quality web search (full content). https://tavily.com
TAVILY_API_KEY=
# Optional: free self-hosted meta-search. e.g. http://localhost:8080
SEARXNG_URL=
# Optional: raises GitHub search rate limits.
GITHUB_TOKEN=
# Optional: neural search (reserved for future provider).
EXA_API_KEY=
